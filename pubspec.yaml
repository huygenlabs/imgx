name: imgx
description: "Flutter package for loading images from URLs (PNG, JPG, WEBP, GIF) with memory/disk caching, expiration, retry mechanism, and progress tracking for smooth server handling."
version: 0.1.5
homepage: "https://github.com/huygenlabs/imgx"

environment:
  sdk: ^3.5.4
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.4.0
  shared_preferences: ^2.5.3
  logger: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.6

topics:
  - image
  - image-loading
  - image-caching
  - image-download
  - image-retry
